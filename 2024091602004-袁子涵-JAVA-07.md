1.Error类型的例子：

（a）java.lang.VirtualMachineError，即Java虚拟机错误。当Java虚拟机崩溃或用尽了继续操作所需的资源时，会报出该错误。

（b）java.lang.StackOverflowError，即栈溢出错误。当应用程序递归太深而发生堆栈溢出时，会报出该错误。

（c）java.lang.OutOfMemoryError，即内存溢出。内存溢出或没有可用的内存提供给垃圾回收器时，会报出该错误。

对于Error类型的错误，java程序不能察觉，也无法解决，当Error错误出现时，程序会立即崩溃，且java虚拟机也会立即停止运行。

Exception类型的例子：

（a）运行异常：

AirthmeticException:算术异常

NullPointerException:空指针异常

（b）编译异常：

TimeoutException:超时异常

ClassNotFoundException:类找不到异常

对于Exception类中的运行异常，java编译器一般不会检查它，当程序中可能出现运行异常，也会被编译通过。

对于编译异常，java编译器一定会检查它并进行处理，若不处理，程序则不能编译通过。

2.checked异常即运行异常，unchecked异常即编译异常，区别如第一问回答所示。